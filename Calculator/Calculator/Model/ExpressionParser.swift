enum ExpressionParser {
    func parse(from input: String) -> Formula {
        let formula = Formula()
        
        return formula
    }

    private func componentByOperators(from input: String) -> [String] {
        return []
    }
}
